

Linux基础名词

命令解析器shell：

默认运行于计算机系统终端的用来解析用户输入命令的工具

内核：
操作系统的核心，笼统的将系统称之为内核

用户通过图形界面控制应用层

应用层通过shell命令翻译到shell层

shell层通过系统调用到内核层

内核层通过驱动指令控制s硬件

根目录的子目录：

/bin:用来存放二进制可执行文件，date,ls,cat,echo...

/root:存放驱动Linux的文件

/dev:存放硬件设备对应的文件。鼠标，键盘...

在Linux中所见皆是文件

